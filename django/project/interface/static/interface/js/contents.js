$(document).ready(function () {
    /*$('#file').on('change', function(){
        var fullPath = document.getElementById('file').value;
        var filename = fullPath.split('\\').pop();
        var fileExtension = fullPath.split('.').pop();

        var preview_row = document.getElementById('preview_row');
        var preview_div = document.getElementById('preview_div');

        if (fileExtension == 'jpeg' || fileExtension == 'png'){
            var reader = new FileReader();
                reader.onload = function()
                {
                    preview_row.removeAttribute('style', 'display')
                    preview_div.innerHTML = '<img id="preview" class="img-preview" src="'+reader.result+'"/>'
                }
                reader.readAsDataURL(event.target.files[0]);
        } else {

        }
    });*/
});